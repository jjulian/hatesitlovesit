!!!
%html{:xmlns => "http://www.w3.org/1999/xhtml"}
  %head
    %meta{:content => "text/html; charset=UTF-8", "http-equiv" => "Content-Type"}/
    %meta{ :name => 'robots', :content => 'noindex, nofollow' }
    = csrf_meta_tag
    %title Hates It! Loves It!
    = stylesheet_link_tag 'application'
    = yield :head
  %body
    - if current_user
      = image_tag current_user.twitter_profile.profile_image_url, :size => '36x36'
      Welcome, #{current_user.screen_name} | (#{link_to 'sign out', sign_out_path})
    = yield
    
    - if Rails.env.development?
      = javascript_include_tag 'jquery-1.4.4.js'
    - else
      = javascript_include_tag 'http://ajax.googleapis.com/ajax/libs/jquery/1.4.4/jquery.min.js'
    = javascript_include_tag :defaults